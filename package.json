{
  "name": "prostate-map",
  "version": "0.2.0",
  "private": false,
  "license": "(ISC OR GPL-3.0)",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "babel": {
    "presets": [ "@babel/react", "@babel/preset-env" ],
    "plugins": ["@babel/plugin-proposal-class-properties"]
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Tesseract-MI/prostate-map.git"
  },
  "dependencies": {
    "react": "^16.9.0",
    "react-dom": "^16.9.0",
    "react-scripts": "3.1.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "publish:npm": "set NODE_ENV=production && rm -rf dist && mkdir dist && npx babel src/prostate-map --out-dir dist --copy-files"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@babel/cli": "^7.5.5"
  }
}
